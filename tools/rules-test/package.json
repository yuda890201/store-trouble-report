{
  "name": "store-trouble-report-rules-test",
  "private": true,
  "version": "1.0.0",
  "description": "firestore.rules がねらいどおりに効いているかを、エミュレータ相手に確かめる",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-rules \"node test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
